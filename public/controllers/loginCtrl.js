var app = angular.module('readingGoals');

app.controller('loginCtrl', function($scope){
//////////// FIREBASE ////////////
// var myFirebaseRef = new Firebase("https://readable.firebaseio.com/");

// var firebaseUsers = $firebaseArray(myFirebaseRef);

//   $scope.users = firebaseUsers;
//   $scope.users.$loaded().then(function(){
//   });

//   $scope.addUser = function(user) {
//     firebaseUsers.$add({ name: user }).then(function(addedUserRef) {
//       var id = addedUserRef.key();
//       var index = firebaseUsers.$indexFor(id);
//     });
//   };
//   $scope.eventSources = [];
});